import { StyledAppBar } from './AppBar.styled';
import { Navigation } from '../Navigation/Navigation';

export const AppBar = () => {
  return (
    <StyledAppBar>
      <Navigation />
    </StyledAppBar>
  );
};
